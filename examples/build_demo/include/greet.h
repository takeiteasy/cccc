#ifndef GREET_H
#define GREET_H

// Returns a greeting for `name` (static buffer; not thread-safe).
const char *greet(const char *name);

// Sums an array of integers.
int sum(const int *xs, int n);

#endif // GREET_H
