#pragma once

// Pulls in the shared header under a `./`-prefixed spelling that resolves
// against *this* file's directory, producing a second path-string key for
// the same physical file. See pragma_once_alias_1277.h.
#include "./pragma_once_alias_1277.h"
